# 预订订单服务 — 后台服务需求

> **服务名称**: booking-service  
> **技术栈**: Spring Boot 3 + MyBatis-Plus + MySQL + Redis  
> **职责**: 预订订单管理、冲突检测、会议签到、消息通知

---

## 功能优先级

| 优先级 | 功能 | 说明 |
| :--- | :--- | :--- |
| **P0** | 创建预订（含冲突检测） | 核心业务 |
| **P0** | 预订列表/详情/取消 | 核心业务 |
| **P1** | 会议签到 | 运营需要 |
| **P1** | 消息通知 | 体验优化 |

## 范围边界

| 范围内 | 范围外 |
| :--- | :--- |
| 预订 CRUD + 冲突检测 | 会议室数据管理（属 room-service） |
| 签到 + 超时自动缺席 | 周期性会议（v1.1） |
| 通知推送 | 审批流程（v1.1） |

## 依赖关系

| 依赖 | 说明 |
| :--- | :--- |
| room-service | 校验会议室是否存在及可用 |
| MySQL | 数据持久化 |
| Redis | 会议室实时状态缓存 + 分布式锁 |

---

## 接口清单

| 方法 | 路径 | 说明 | 优先级 |
| :--- | :--- | :--- | :--- |
| POST | /api/bookings | 创建预订（含冲突检测） | P0 |
| GET | /api/bookings | 预订列表 | P0 |
| GET | /api/bookings/:id | 预订详情 | P0 |
| DELETE | /api/bookings/:id | 取消预订 | P0 |
| POST | /api/bookings/check-conflict | 检查时段冲突 | P0 |
| POST | /api/checkin/:bookingId | 会议签到 | P1 |
| GET | /api/notifications | 通知列表 | P1 |

## BDD 验收标准

```
Given 会议室 A 在 10:00-11:30 已有预订
When  员工 B 尝试预订 A 的 10:30-12:00 时段
Then  返回冲突提示，推荐最近可用时段

Given 会议开始前 20 分钟
When  预订人扫描该会议二维码
Then  签到成功，返回"已签到"

Given 会议开始 35 分钟后仍未签到
When  系统定时任务执行
Then  该预订自动标记为"缺席"

Given 预订取消成功
When  预订人刷新通知列表
Then  看到一条新的取消通知
```

---

## 冲突检测规则

- 同一会议室、同一时段，不允许重复预订
- 检测精度：分钟级
- SQL 条件：`start_time < :requestEnd AND end_time > :requestStart`
- Redis 分布式锁防止并发冲突

## 业务规则

| 规则 | 说明 |
| :--- | :--- |
| 单人单日上限 | 同一用户每天最多预订 4 个时段 |
| 取消时限 | 会议开始前 30 分钟以上可取消 |
| 签到时间窗 | 开始前 15 分钟 ~ 开始后 30 分钟 |
| 缺席判定 | 开始后 30 分钟未签到，自动标记 |

---

## 数据库：bookings 表

| 字段 | 类型 | 说明 |
| :--- | :--- | :--- |
| id | BIGINT | 主键自增 |
| room_id | BIGINT | 会议室ID（外键） |
| user_id | BIGINT | 预订人ID（外键） |
| title | VARCHAR(200) | 会议主题 |
| start_time | DATETIME | 开始时间 |
| end_time | DATETIME | 结束时间 |
| status | TINYINT | 0=已确认 1=进行中 2=已完成 3=已取消 4=缺席 |
| cancel_reason | VARCHAR(500) | 取消原因 |
| checkin_at | DATETIME | 签到时间 |
| created_at | DATETIME | 创建时间 |

## 数据库：notifications 表

| 字段 | 类型 | 说明 |
| :--- | :--- | :--- |
| id | BIGINT | 主键自增 |
| user_id | BIGINT | 用户ID |
| type | VARCHAR(20) | 通知类型 |
| title | VARCHAR(200) | 通知标题 |
| content | TEXT | 通知内容 |
| is_read | TINYINT | 0=未读 1=已读 |
| created_at | DATETIME | 创建时间 |

---

## 非功能要求

| 类别 | 要求 |
| :--- | :--- |
| 性能 | 冲突检测 < 500ms |
| 并发 | 500 TPS，Redis 分布式锁防并发超卖 |
| 可用性 | 99.9%，Redis 哨兵模式 |
| 安全 | 所有写操作记录审计日志 |

## 术语表

| 术语 | 说明 |
| :--- | :--- |
| 冲突检测 | 同一会议室同一时段不允许重复预订 |
| 分布式锁 | Redis SETNX 实现的并发控制 |
| 哨兵模式 | Redis 高可用方案，自动故障转移 |
| 时间窗 | 允许操作的时间范围 |
