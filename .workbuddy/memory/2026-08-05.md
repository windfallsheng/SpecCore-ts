## 2026-08-05 工作日志

### SpecCore CLI 命令统计
- 确认当前共有 **51 条 `.command()` 注册**（45 唯一名 + 6 个 schedule 子命令）
- 简洁模式展示 17 条核心命令，全量模式展示全部

### 修复 Qcoder `/spec` 命令提示不生效
**根因**: `speccore init` 的 Qcoder 集成有两个 bug:
1. 目录名错误: `.qcoder/` → 应为 `.qoder/`（无 'c'）
2. 缺少 `commands/` 子目录: Qoder 项目级指令路径是 `.qoder/commands/`

**修复** (`src/commands/init.ts`):
- 改为 `.qoder/commands/spec/` 结构，文件去 `spec-` 前缀
- 用 plain markdown 格式（Qcoder 不需要 YAML frontmatter）
- 增加旧 `.qcoder/` 目录自动清理

**当前项目已更新**: 10 个 Qcoder 命令文件位于 `.qoder/commands/spec/`

---

### LLM 集成到 Ask 引擎 (v5.26.1+)
- 新建 `src/core/ask-llm.ts` — 真正的 LLM 意图解析层
- 三层调用策略:
  1. OpenAI 兼容 API (`SPECCORE_LLM_ENDPOINT` + `SPECCORE_LLM_KEY` 环境变量)
  2. Ollama 本地 (`http://localhost:11434`, qwen2.5:7b)
  3. 规则引擎兜底（无 LLM 时自动降级）
- System prompt 包含 18 条命令 KB + 4 种工作流 + 输出 JSON schema
- 集成到 `ask-engine.ts` 的 `askEngine()` — LLM 优先，失败自动降级
- `enrichWithRules()` 函数用规则引擎补充 LLM 返回的内容详情
- TTY 检测: 终端=Unicode框线, AI模式=HTML页面
