# {期次名称} - 总需求文档

> 本文档是本期次的总需求文档。  
> 完成填写后，运行 `/myspec-iteration-split` 自动拆分为 Feature。

---

## 1. 项目背景

{描述本期次的业务背景、目标用户、核心目标}

**示例**：
- 公司会议室预定目前使用微信群接龙，效率低、冲突多
- 目标：开发一套会议室预定系统，支持 Web 和 H5 双端
- 用户：全体员工（普通用户 + 管理员）

---

## 2. 功能模块列表

### 2.1 {模块名称}

- **功能描述**：{描述该功能的核心逻辑和用户价值}
- **优先级**：高 / 中 / 低
- **依赖**：{依赖的其他模块，如"无"或"2.2 会议室管理"}
- **验收标准**：
  - [ ] {验收标准1：具体、可验证的条件}
  - [ ] {验收标准2}

### 2.2 {模块名称}

- **功能描述**：{描述}
- **优先级**：高 / 中 / 低
- **依赖**：{依赖}
- **验收标准**：
  - [ ] {验收标准}

> **提示**：每个模块将成为一个独立的 Feature。模块之间应边界清晰，尽量减少耦合。

---

## 3. 非功能性需求

### 3.1 性能要求
- 接口响应时间：{如：P95 < 200ms}
- 页面加载时间：{如：首屏 < 1s}
- 并发用户：{如：支持 100 人同时在线}

### 3.2 安全要求
- 认证方式：{如：JWT Token + 刷新机制}
- 权限控制：{如：RBAC 模型，区分 admin/普通用户}
- 数据安全：{如：密码 BCrypt 加密，敏感字段脱敏}

### 3.3 兼容性要求
- Web 端：{如：PC 浏览器，Chrome/Firefox/Edge 最新 2 版本}
- H5 端：{如：iOS Safari, Android Chrome}
- 响应式：{如：支持 768px ~ 1920px 宽度}

---

## 4. 约束与假设

### 4.1 技术约束
- {如：必须使用 Spring Boot 3.2 + Vue 3}
- {如：数据库使用 MySQL 8.0}
- {如：缓存使用 Redis 7.0}

### 4.2 业务假设
- {如：用户默认已通过企业 SSO 注册}
- {如：会议室信息由管理员维护，普通用户只读}

---

## 5. 风险与依赖

### 5.1 外部依赖
- {如：需对接企业微信 SSO 获取用户信息}
- {如：需申请短信服务发送验证码}

### 5.2 已知风险
- {如：并发量可能超出现有架构承载，需提前压测}
- {如：第三方 SSO 服务不稳定，需设计降级方案}

---

## 6. 原型与参考

- {原型链接或附件位置}
- {竞品参考链接}
- {设计稿位置}

---

> **下一步**：填写完成后，运行 `/myspec-iteration-split` 自动拆分为 Feature。
