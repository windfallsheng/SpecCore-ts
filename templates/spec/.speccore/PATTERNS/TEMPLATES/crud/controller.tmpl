/**
 * @spec {Task编号}
 * @spec-link {Spec文件路径}
 * @spec-title {任务名称}
 * @spec-type backend
 * @generated-by SpecCore Template: CRUD Controller
 * @template-id T-001
 */
@RestController
@RequestMapping("/api/v1/{apiPath}")
@RequiredArgsConstructor
@Slf4j
@Validated
public class {EntityName}Controller {

    private final {EntityName}Service {entityName}Service;

    /**
     * 创建
     */
    @PostMapping
    public Result<{EntityName}DTO> create(@Valid @RequestBody {EntityName}CreateReq req) {
        log.info("创建{entityName}，参数：{}", req);
        {EntityName}DTO result = {entityName}Service.create(req);
        return Result.success(result);
    }

    /**
     * 分页查询
     */
    @GetMapping("/page")
    public Result<PageResult<{EntityName}DTO>> page(@Valid {EntityName}PageReq req) {
        log.info("分页查询{entityName}，参数：{}", req);
        PageResult<{EntityName}DTO> result = {entityName}Service.page(req);
        return Result.success(result);
    }

    /**
     * 详情
     */
    @GetMapping("/{id}")
    public Result<{EntityName}DTO> detail(@PathVariable Long id) {
        log.info("查询{entityName}详情，id：{}", id);
        {EntityName}DTO result = {entityName}Service.detail(id);
        return Result.success(result);
    }

    /**
     * 更新
     */
    @PutMapping("/{id}")
    public Result<{EntityName}DTO> update(@PathVariable Long id, @Valid @RequestBody {EntityName}UpdateReq req) {
        log.info("更新{entityName}，id：{}，参数：{}", id, req);
        {EntityName}DTO result = {entityName}Service.update(id, req);
        return Result.success(result);
    }

    /**
     * 删除
     */
    @DeleteMapping("/{id}")
    public Result<Void> delete(@PathVariable Long id) {
        log.info("删除{entityName}，id：{}", id);
        {entityName}Service.delete(id);
        return Result.success();
    }
}
